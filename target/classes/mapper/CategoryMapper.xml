<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.budget.dao.CategoryDao">

    <select id="selectAll" resultType="Category">
        SELECT category_id, category_name, category_type, icon, color
          FROM category
         ORDER BY category_type ASC, category_id ASC
    </select>

    <select id="selectByType" parameterType="String" resultType="Category">
        SELECT category_id, category_name, category_type, icon, color
          FROM category
         WHERE category_type = #{categoryType}
         ORDER BY category_id ASC
    </select>

    <select id="selectOne" parameterType="int" resultType="Category">
        SELECT category_id, category_name, category_type, icon, color
          FROM category
         WHERE category_id = #{categoryId}
    </select>

</mapper>
